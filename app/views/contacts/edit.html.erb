<h2><%= @contact.name %></h2>
<%= @contact.content %>
<%= @contact.email %>
<%= @contact.phone %>
<%= form_with model: @contact, local: true do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>